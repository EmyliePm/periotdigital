import Hero from "@/components/home/Hero";

export default function Home() {
  return (
    <main>
      <Hero />

      <section
        id="work"
        style={{
          minHeight: "100vh",
          background: "#020306",
        }}
      />
    </main>
  );
}
